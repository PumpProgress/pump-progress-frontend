import 'package:flutter/widgets.dart';

class WorkoutSessionItemWidget extends StatelessWidget {
  const WorkoutSessionItemWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
