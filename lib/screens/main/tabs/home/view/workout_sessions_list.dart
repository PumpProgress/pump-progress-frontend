import 'package:flutter/material.dart';

class WorkoutSessionsListWidget extends StatelessWidget {
  const WorkoutSessionsListWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("Work in progress...");
  }
}
